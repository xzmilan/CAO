env_config:
  default_environment: dev
  environments:
  - name: dev
    env:
      DBT_CURRENT_WH: COMPUTE_WH
      DBT_CURRENT_DB: CUSTOMER_JOURNEYS
      DBT_CURRENT_SCHEMA: JOURNEY
      DBT_CURRENT_ROLE: ACCOUNTADMIN