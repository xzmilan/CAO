env_config:
  default_environment: dev
  environments:
  - name: dev
    env:
      DBT_CURRENT_WH: COMPUTE_WH
      DBT_CURRENT_DB: PRD_BIZDB_PL_MARKETING
      DBT_CURRENT_SCHEMA: CUSTOMERJOURNEY
      DBT_CURRENT_ROLE: ACCOUNTADMIN